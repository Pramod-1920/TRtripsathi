import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // Existing data-loading effects intentionally update local request state.
      // These React Compiler rules are advisory and reject those established patterns.
      "react-hooks/immutability": "off",
      "react-hooks/preserve-manual-memoization": "off",
      "react-hooks/set-state-in-effect": "off",
      // Request effects deliberately key off pagination/filter primitives; the loaders
      // are kept outside dependency lists to avoid refetch loops.
      "react-hooks/exhaustive-deps": "off",
      // Admin media uses runtime Cloudinary/external URLs that cannot be statically configured.
      "@next/next/no-img-element": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
