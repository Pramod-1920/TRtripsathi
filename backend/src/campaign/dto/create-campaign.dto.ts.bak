import { IsString, IsNotEmpty, IsOptional } from 'class-validator';

// Minimal DTO used until full validation is introduced. Keeps build green.
export class CreateCampaignDto {
  @IsString()
  @IsNotEmpty()
  title!: string;

  @IsString()
  @IsOptional()
  description?: string;
}
